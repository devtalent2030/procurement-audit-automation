theme: jekyll-theme-cayman


title: Procurement Audit Automation
description: Config-driven ERP audit engine with FOIP/PII risk scanning (NLP) and audit-ready reporting outputs.


# Small UX upgrades
show_downloads: false


# Helps GitHub Pages generate correct canonical links + metadata
url: https://devtalent2030.github.io
baseurl: /procurement-audit-automation


# Optional: adds nicer metadata cards when someone shares your site link
plugins:
- jekyll-seo-tag
- jekyll-sitemap


# Markdown engine (default is fine, but being explicit avoids surprises)
markdown: kramdown

sass:
  sass_dir: assets/css
  style: compressed

# Optional: if you later want nav links in a header (we can add this after content)
# nav:
# - label: Home
# link: /
# - label: Demo
# link: /demo